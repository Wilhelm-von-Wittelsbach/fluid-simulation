import flusim.simulator

class Fluid:
    """`Fluid` holds information about fluid and PDE used for simulation.
    Args:
        fluid_prop (dict): FLuid properties.
        boundary_cond (undecided)
        init_cond (numpy.array): A tensor that contains initial velocities at t=0.
        simulator (Simulator): Simulation strategy.
    """

    def __init__(self):
        pass
